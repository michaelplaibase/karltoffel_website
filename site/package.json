{
  "name": "karltoffel-tilbudsmotor",
  "version": "1.0.0",
  "private": true,
  "description": "Karltoffel tilbudsmotor - production build (static frontend + Express API)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file=.env src/server/index.js",
    "start:noenv": "node src/server/index.js",
    "dev": "node --env-file=.env --watch src/server/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.0.0",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "nodemailer": "^6.9.16"
  }
}
